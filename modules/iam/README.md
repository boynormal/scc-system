# Module: `iam`

ผู้ใช้, บทบาท, session boundary — เป้าหมายย้าย `app/api/users/**`, flow สิทธิ์ที่เกี่ยว identity มาที่นี่

โครงสร้างมาตรฐาน: `domain/` → `application/` → `infra/` → `ui/` (เลือกใช้ตามความจำเป็น)
